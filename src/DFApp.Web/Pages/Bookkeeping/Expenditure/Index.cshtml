@page
@model DFApp.Web.Pages.Bookkeeping.Expenditure.IndexModel
@using DFApp.Localization
@using DFApp.Permissions
@using Microsoft.AspNetCore.Authorization
@using Microsoft.Extensions.Localization
@inject IStringLocalizer<DFAppResource> L
@inject IAuthorizationService AuthorizationService
@section scripts {
    <abp-script src="/Pages/Bookkeeping/Expenditure/Index.js"></abp-script>
}

<abp-card>
    <abp-card-header>
        <abp-row>
            <abp-column size-md="_6">
                <abp-card-title>@L["BookkeepingExpenditure:Title"]</abp-card-title>
            </abp-column>
            <abp-column size-md="_6" class="text-end">
                @if(await AuthorizationService.IsGrantedAnyAsync(DFAppPermissions.BookkeepingExpenditure.Create))
                {
                    <abp-button id="NewExpenditureButton"
                                text="@L["NewBook"].Value"
                                icon="plus"
                                button-type="Primary" />
                }
            </abp-column>
        </abp-row>
        
    </abp-card-header>
    <abp-card-body>
        <abp-table striped-rows="true" id="ExpenditureTable"></abp-table>
    </abp-card-body>
</abp-card>